machine:
  python:
    version: 3.6.0

dependencies:
  override:
    - pip install -r requirements/dev.txt

test:
  override:
    - isort -rc -c workflow
    - flake8
    - py.test
  post:
    - cp -r coverage $CIRCLE_ARTIFACTS
